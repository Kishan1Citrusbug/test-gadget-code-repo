/**
* Validation code for field name on Product
* @param {import("gadget-server").ProductNameFieldValidationContext } context - All the useful bits for running this validation.
*/
module.exports = async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.name` to get the fields's data
  // add to the `errors` with `errors.add('name', "did not pass the validation")`
};